# RESEARCH.md — 研究笔记

> 本文件记录**这个项目要回答的问题**、**已经验证的结论**和**还没搞清楚的未知**。
> 它和另外两份文档的分工：
>
> - [docs/roadmap.md](docs/roadmap.md) — 权威设计文档，写的是**已经定下来的决策**
> - [AGENTS.md](AGENTS.md) — 代理工作指南，写的是**怎么干活**
> - **本文件** — 写的是**还不知道答案的东西**，以及得出结论的过程
>
> **写作规则**：结论必须标注来源等级。没验证过的猜测一律写"待验证"，不要写成事实。
>
> | 标记 | 含义 |
> |---|---|
> | ✅ **已实测** | 在本仓库里跑出来的结果，附命令或文件:行号 |
> | 📖 **有出处** | 外部资料，附链接，但**本仓库未复现** |
> | 🤔 **待验证** | 推理或经验判断，还没做实验 |
> | ❌ **已推翻** | 曾经以为是，实验证明不是（保留，避免重蹈覆辙） |

---

## 1. 项目要回答的核心问题

按对成品质量的影响排序。**上面的没答案之前，不要去做下面的。**

| # | 问题 | 为什么重要 | 状态 |
|---|---|---|---|
| Q1 | 双摇杆的手感参数是多少？ | 技术路线 §10 明确：手感定不下来就铺内容，改一次要重调所有敌人 | 🔧 **调参台已就绪（F2），等实测**<br>✅ §3.10 已修，护盾五个参数也进了调参台 |
| Q2 | 房间地形怎么从"矩形竞技场"变成"像设计过的地牢"？ | 模板库已让地形不再是矩形（§3.9），但只剩两个问题：9 张够不够、ASCII 模板何时转 `.tscn` | ✅ **模板库已落地**（9 张，§3.9）；待实测重复感 |
| Q3 | 敌人 AI 什么时候该从手写 `_steer()` 换成行为树？ | 决定 Beehave/LimboAI 的引入时机，过早引入是负担 | 🤔 未开始 |
| Q4 | 2D 灯光能不能在这个规模下跑满帧？ | 技术路线 §10 说这是"性价比最高的观感提升"，但没验证过性能 | ✅ **性能已答**（§3.11：169.8 vs 166.4 FPS，不是瓶颈）；观感待用户看 |
| Q5 | 弹幕量上来之后，子弹要不要换成对象池？ | 现在每发子弹都 `instantiate` + `queue_free` | 🤔 未开始 |
| Q6 | 元进度要设计成什么样才算"死一次能感到变强了"？ | M4 的验收标准，但现在的设计（3 个扁平升级）明显不够 | 🤔 未开始 |

### 推进方式（2026-09-18 与用户确认）

| 项 | 决定 |
|---|---|
| Q1 分工 | **我做调参台，用户来玩。** 主观手感只能由人判定；我负责把值固化进代码并补测试 |
| Q2 范围 | **先做小步：模板库**（保持链式布局，先做固定尺寸 + 手搭模板随机抽取），不做 5×5 中心入口大改 |
| 汇报粒度 | **做完一个里程碑汇报**，不是每小步都停 |

**里程碑 1（已完成）**：修 P0 + P2、清死代码、交付调参台、补回归测试。
评测指标：两套测试全绿、真实渲染下开局不黑屏。

**里程碑 2（已完成）**：Q2 小步——房间模板库。
9 张手搭 ASCII 模板、固定 15/21/25 尺寸、两条不变量自检（外圈留空 / 内部连通）。
过程中发现并修复了一个**先前就存在的 off-by-one**（瓦片层整体偏离一格，
门被开在地板而非墙上），见 §3.9。评测指标：两套测试 138 项全绿。

后续里程碑（3 灯光、4 布局、5 小地图、6 M2 完整循环）按日期记在 §7。

---

## 2. 对标拆解：元气骑士（Soul Knight）

### 2.1 已知的具体数据

> 来源为社区 wiki（📖 **有出处，但未经本项目实测**）。中文名/数值可能与当前版本有出入，
> **改动生成器前请先开游戏核对**。出处见 §6。
>
> 来源：<https://soul-knight.fandom.com/wiki/User_blog:ReChording/Room_Patterns_and_Generation>

| 维度 | 元气骑士 | 本项目当前 |
|---|---|---|
| 房间网格 | **5×5**，入口房间在**正中心** | ✅ 5×5，入口在正中（`GRID_SIDE` / `GRID_CENTRE`） |
| 房间中心间距 | 40 tiles | 由每列最宽房间累积决定 |
| 走廊宽度 | 5 tiles | 门 = 1 tile（`TILE = 16`） |
| 房间尺寸 | 敌人房 15 / 21 / 25 tiles | ✅ **15 / 21 / 25，正方形，固定池**（`_plan_sizes()`） |
| 入口房 | 11×11 | 无专用尺寸，同样从 15/21/25 里随机取 |
| 出口房 | 11×11；BOSS 房 21×21 | 与普通房同尺寸 |
| 每层房间数 | ≥5（含入口与出口），≥2 敌人房 | ✅ 1 层 5 间、2 层 7 间（探针抽样），`≥5` 由 `test_run` §20 守着 |
| 到出口的最短路径 | x-1~x-4 必经 **2** 个敌人房；x-5 必经 **3** 个 | ✅ 必经 2（普通层）/ 3（BOSS 层），`required_enemies`，按**走出来的**最短路线断言 |
| 分支深度 | 通常只延伸 1 个房间 | 挂 1 个房间 |
| 武器总数 | 270+ | 6 |
| 护盾 | 类似 Halo 的自动回复护盾 | ✅ 延迟 3.5 秒后按 `rate` 逐点回复（帧率无关，§3.10） |
| 能量 | 限制强力武器；**建议留一把不耗能的武器** | ✅ 已实现（`EnergyPool`，近战 0 耗能） |
| 楼层间成长 | 每层提供可叠加的 buff 选择 | ❌ 无 |
| 角色差异 | 血量/护甲/能量/冷却技能各不同 | ❌ 只有 `knight` 一个 |

### 2.2 值得借鉴的三条

1. **入口在正中心，出口在边缘** —— ✅ **已实现**（2026-09-18，见 §4.1 Q2.2）。
   四个方向都可能通往新房间，空间感明显强于"从左往右一条线"。
   出处那句"玩家一开始就能看到往哪走"和中心入口其实是矛盾的——方向不再自明。
   这一条**已用左上角小地图补上**（`scripts/ui/minimap.gd`：画整层房间图 + 门连线 +
   当前房白框 + 出口金框，`Level.room_changed` 驱动）。
   刻意**不做**成"往哪走"的箭头：那会把选择权收回去，而选择权正是这次重构的目的。

2. **最短路径上的敌人房数量是硬约束**（2 个 / BOSS 层 3 个）—— 保证节奏，
   而不是由随机数决定玩家这一层有多累。✅ **已实现**：`_plan_layout()` 的
   `required_enemies` 就是这个数。旧实现是"主干全段都是 COMBAT"，节奏偏重且
   不可设计。关键细节见技术路线 §7"主干必须是一棵树"——不保证门图是树的话，
   这条约束会被一道多余的捷径门悄悄废掉。

3. **"留一把不耗能的武器"是设计出来的，不是平衡出来的** ——
   本项目已经踩到了这一点（`sword` 耗能 0），技术路线 §6 也写了"不要平衡掉"。
   **和原作一致，保持。**

### 2.3 不该照抄的两条

1. **移动端的自动瞄准** —— 原作右摇杆是静态按钮 + 自动索敌，这是**移动端妥协**。
   PC 上有鼠标，双摇杆自由瞄准本身就是卖点。本项目的 `_read_aim_direction()` 是对的。

2. **270+ 武器的数量** —— 那是多年运营的结果。原型阶段 6 把能跑的武器
   比 30 把换皮的武器有用得多。

---

## 3. 已验证的结论（附复现方式）

> 这些结论在 [docs/roadmap.md §11](docs/roadmap.md) 有更完整的叙述，
> 这里只记录**结论 + 怎么验证的**。

### 3.1 关卡主干必须只走正交步 ✅

- **结论**：主干用斜向步进（`(1,1)`）会挖不出门 → 通关路径断裂。
  压力测试：200 个种子里 **141 个**生成无法通关的楼层。
- **现状**：已修复，由 `tools/test_run.gd` §12 守着（跨 60 个种子验证
  无重叠 / 全可达 / 门数足够）。
- **复现**：`$GODOT --headless --path . res://tools/test_run.tscn`，看 §12 三项。

### 3.2 子弹 mask 必须按阵营区分 ✅

- **结论**：玩家子弹与敌人子弹共用 `mask = 16` 时，**敌人子弹永远打不中玩家，
  且完全不报错**。
- **现状**：已修复，`Projectile` 按 `is_player_team` 设 mask。
- **复现**：`test_run` §6「子弹不打自己人」与 §7「敌人子弹能打中玩家」。

### 3.3 两层测试都是必需的 ✅

- **结论**：核心套件（`test_gameplay`）**抓不到**关卡重叠、门指向错误、
  敌人卡墙、子弹打到自己人这类问题——这些只有真实跑一局才暴露。
- **证据**：两套测试分工明确，`test_run` 的 §5~§11 全是"真物理"验证。
- **推论**：新增任何涉及**空间/物理/时序**的机制，都要往 `test_run` 加断言，
  而不是只加 `test_gameplay`。

### 3.4 现有测试覆盖不到的盲区 ✅（已修复）

- **结论**：**两套测试都完整跑通了，仍然漏掉了一个 P0 级致命故障。**
- **具体**：`SceneRouter.go_to_scene()` 读写不存在的属性 `GameState.current_scene_path`，
  运行时抛 `SCRIPT ERROR`。因为崩溃发生在**淡出到全黑之后**，
  表现是主菜单按「开始」→ 黑屏卡死，`SceneRouter._busy` 与 `get_tree().paused` 永久为 `true`。
- **为什么测试没抓到**：两套测试都只 `_check(SceneRouter != null)`（存在性），
  **从没调用过 `start_new_run()` 或 `go_to_scene()`**。存在 ≠ 可用。
- **状态**：**已于 2026-09-18 修复**，并加了 `test_run.gd` §14 驱动真实入口的回归测试。
- **教训（写进方法论，见 §5.4）**：**测试要覆盖"入口路径"，不能只验证对象存在。**

### 3.5 退出码不可作为唯一判据 ✅

- **结论**：`quit(1)` **会**传出退出码 1，所以"断言失败仍返回 0"的说法**不成立**；
  真正危险的是**脚本中途报错、没走到 `quit()` 时返回 0**。
- **实测**（探针，已删）：

  | 情形 | 退出码 |
  |---|---|
  | `get_tree().quit(1)` | **1** |
  | 调不存在的属性抛错后未 quit | **0** |

- **推论**：必须读 stdout 的 `ALL NN CHECKS PASSED`。P0 恰好属于第二类 ——
  既不报红也不改退出码，两条判据同时失效。
- **修法**：`AGENTS.md §1` 已更正（原先的理由写错了，结论是对的）。

### 3.6 永久升级的定义必须只有一处 ✅

- **结论**：升级"卖什么"和"应用什么"分成两张手写列表时，
  漏一个就是**静默的**（玩家花掉宝石，属性没变）。
- **证据**：P2 —— `main_menu.gd` 卖 `speed`，`game.gd` 只应用
  `max_health` / `max_armor`。两套测试都没覆盖"买了之后属性真的变了吗"。
- **修法**：收拢到 `GameState.UPGRADES` 单表，购买价/上限/应用全部从表里读；
  `Player.apply_meta_upgrades()` 按表应用。
  回归测试 `test_run.gd` §15 对**每一项**升级断言属性真的变了。
- **可推广的模式**：凡是"定义在 A 处、消费在 B 处"的成对逻辑，
  都要问一句**"两边会分叉吗"**。反射/表单驱动（如调参台）是同一个模式的解法。

### 3.7 恢复全局状态的逻辑必须只有一处 ✅

- **结论**：`paused` / `input_locked` / `_busy` 的恢复散落在成功路径和
  `_abort()` 两处时，重构成功路径**很容易漏掉一个** → 游戏停摆但路由自称空闲。
- **证据**：修 P0 时我重构了 `go_to_scene()`，成功路径丢了 `paused = false`，
  当场被 `test_run` §14 抓到（`tree is not left paused` FAIL）。
  **新加的回归测试立刻抓到了新引入的回归。**
- **修法**：收拢成唯一的 `_release()`，成功与失败路径共用。
- **设计原则**：**可能失败的操作放在修改全局状态之前**，
  这样"失败"只能是提前 return，而不是"卡在半途"。

### 3.8 `ui_*` 是引擎内置动作，删掉无害但会误导 ✅

- **结论**：`ProjectSettings` 里 90+ 个 `ui_*` 是引擎内置 UI 导航动作，
  用 `set_setting(name, null)` 从 `project.godot` 删掉后，
  **运行时 `InputMap` 会自动恢复默认绑定**（实测 `ui_accept` 3 个事件、
  `ui_cancel` = Escape）。
- **风险不在功能**，而在文件内容不可预测，且会静默丢弃用户在编辑器里手动做的绑定。
- **推论**：清理输入动作要用**显式退役清单**，不要用"删除所有不在表里的"。

---

### 3.9 瓦片层坐标曾整体偏离一格（模板库期间发现）✅

- **结论**：`Room._build_tiles()` 把墙环画在 layer `-1` 和 `interior+1`，
  而房间局部坐标（`interior_rect()` 从 `(16,16)` 起、`world_size()` 为
  `interior+2` 格）隐含的约定是墙环在 layer `0` 和 `interior+1`。
  **每个瓦片都比其余代码认为的位置偏了一格。**
- **后果**：门被开在**地板**上而不是墙上（`Door0` 出现在 `local y=-8`，
  即房间矩形之外）；一切由 `interior_rect()` / `world_size()` 推导的位置
  （出生点、敌人落位、门的对齐）都差一格。
- **为什么长期没被发现**：这是一个**静默的 off-by-one**。
  两套测试当时**全绿**——逻辑测试断言的是"门存在且指向正确房间"、
  "玩家在房间矩形内"，这些在偏一格时依然成立。
  只有把几何量出来（`layer.get_used_rect()` 对比 `world_size()`）才暴露。
- **怎么抓到的**：给模板库写"贴出来的瓦片必须和模板一致"的断言
  （`test_run` §17），首次运行报 355 处不匹配。
- **顺带修的两个连带问题**：
  1. 门触发框原本只有 1 格、且正好嵌在墙里，玩家撞墙时身体边缘只与它
     **相切**，重叠判断不可靠 → 时灵时不灵。改成 2 格长、跨墙与地板各一格。
  2. 敌人生成原本是"房间矩形内随机取点"。以前全是空地所以没事，
     模板出现柱子后会把敌人放进墙里 → 卡住打不死 → **房间永远清不掉**。
     改成用模板的可走格并要求距边界 ≥2 格（避开门口）。
- **教训（写进方法论）**：**格子坐标差一格是静默的。**
  涉及空间的断言要**直接量几何**（`used_rect`、世界坐标、格子索引），
  不要断言"结构关系"——关系在偏一格时往往依然自洽。
- **复现**：`test_run` §17「stamped tiles match their template」，
  以及技术路线 §7.2 的坐标约定表。

### 3.10 `armor_regen_rate` 从来没生效过，护盾是按帧回的 ✅

- **结论**：`Health._process()` 在安静期结束后**每帧** `armor + 1`，
  完全没读 `armor_regen_rate`。那个参数被 `@export`、被 `player.tscn` 设成 1.0、
  被测试设成 1.0，**消费者数量是 0**。
- **实测**（探针，已删）：`delay=0 / rate=1.0 / maximum_armor=5`，打空护甲后逐帧采样
  得到 `[1,2,3,4,5]` —— **5 帧、0.021 秒**回满，折合**每秒 238 点**；
  而参数注释承诺的是"每秒 1 点"（5 点要 5 秒）。
- **推论**：回满耗时等于 `maximum_armor` 帧，**与帧率挂钩**，144 Hz 上比 60 Hz 快
  2.4 倍。玩家实配 `armor_regen_delay = 3.5`，所以实战观感是"躲 3.5 秒 → 瞬间满盾"，
  而护盾的设计意图是奖励拉开距离（roadmap §6），现在它奖励的是找掩体站着不动。
- **测试为什么没抓到**：`test_gameplay` §4 只断言 `armor >= 1`，每帧 +1 下恒成立。
  这是 §5.4 那条教训的又一个实例——**断言太弱等于没断言**。
- **状态**：**已修**（2026-09-18，用户选逐点回复）。`Health` 加 `_regen_progress`
  按 `rate * delta` 攒小数，满 1 点才 +1，与帧率无关；玩家起始值 `rate = 2.0`。
  实现细节与边界见 [AGENTS.md §6 P4](AGENTS.md)。
- **回归测试**：`test_gameplay` §4 新增一条比较式断言（两个只差 rate 的盾跑同一段时间）。
  **反向验证做过**：临时把实现改回每帧 +1，该断言报 `slow 3 / fast 3` 并失败 ——
  即它在修复前确实是红的，不是那种"永远绿"的假测试。
- **复现**：`$GODOT --headless --path . res://tools/test_gameplay.tscn`，
  看 §4 的 `regen speed comes from the rate, not the frame count` 一项。
  （原先写的复现方式是"`grep -rn armor_regen_rate` 只有声明与赋值"，
  修复后 `health.gd` 已有读取方，那句话已经不成立，故改指测试。）

### 3.11 2D 灯光在这个规模下不是瓶颈 ✅

- **结论**：一层 5 房 / 15 个光源（含玩家随身灯）/ 41 个遮挡物，
  **邻近开关 169.8 FPS，全部常开 166.4 FPS**，差 2%。
  环境：RTX 4060 笔记本、480×270 逻辑分辨率、Forward+。
- **怎么测的**：探针（已删）**带窗口**跑真实渲染，各测 180 帧的墙钟时间。
  注意 headless 没有光栅化，那种模式下的 FPS 数字**没有意义**，这类测量必须开窗口。
- **推论**：邻近开关现在省不到几帧，但**保留**——它把成本从"随楼层房间数增长"
  变成"随玩家邻居数增长"，要等房间和弹幕变多才兑现。
  别因为"只值 2%"删掉它，也别以为它此刻在救帧率。
- **顺带实测的两个引擎事实**（叙述见技术路线 §11.4 第 27、28 条）：
  `TileMapLayer` 在 4.7 **没有任何 per-tile 遮挡物接口**；
  新增 `class_name` 脚本必须先跑一次 `--editor --quit` 才会被注册。
- **还没答的**：**观感**好不好看只能由人判。截图上迭代过一轮
  （环境光 0.30 → 0.14、火把能量 1.9、每房至少 2 个），才读得出"一池一池的火把"
  而不是一片均匀的暗。
- **复现**：`$GODOT --headless --path . res://tools/test_run.tscn` 看 §19 的五项。

### 3.12 父节点 `_ready` 里改 `Health.maximum` 不会改 `current` ✅

- **现象**：BOSS 的血量在 `Boss._ready()` 里按层数设置。`Health` 是子节点，
  `_ready()` **先于**父节点执行，`current` 已按场景里的默认 `maximum`（5）填充；
  父节点随后只改 `maximum` 的话，BOSS 实际只有 5 点血，且**不报任何错**。
- **修法**：用现成的 `Health.set_maximum(value, refill := true)`，它同时钳制
  `current` 并发 `changed` 信号。不要手写 `maximum = ...; current = ...` 两行。
- **同类面**：任何"子节点组件在 `_ready` 里自初始化、父节点在 `_ready` 里覆写"
  的组合都有这个时序。覆写要么走组件提供的 setter，要么放到 `setup()` 这类
  显式初始化入口里做（`Room.setup()` 就是这个模式）。

## 4. 待验证的未知

### 4.1 关卡生成（最高优先级）

| # | 问题 | 打算怎么验证 |
|---|---|---|
| Q2.1 | 手搭房间模板需要多少张才不觉重复？ | **已有 9 张**（3 尺寸 × 3 布局）。跑 30 局记录主观重复感——**这项要用户来判** |
| Q2.2 | 5×5 网格 + 中心入口，比现在的链式好在哪？ | ✅ **已实现**（2026-09-18）。入口恒在正中、出口恒在边缘、走出来的最短路线必含 2~3 个战斗房，跨 30 种子两种奇偶验证（`test_run` §20）。**A/B 对比没做**——旧链式实现已被替换，需要时从 git 取 |
| Q2.3 | 房间尺寸固定（15/21/25）还是继续随机？ | ✅ **已定为固定**。门的对齐依赖墙面重叠量，固定尺寸让 `_connect()` 精确；代价是外形变化变少 |
| Q2.4 | 分支该不该更深？现在只挂 1 个房间 | 对比 1 vs 2 的探索欲望与走冤枉路的烦躁度 |

> **已照此执行**：Q2.2 采纳后重写了 `Level._plan_layout()`，
> 先确认 `test_run` §12 的种子压力测试在跑再动生成器——它确实兜住了
> （改动过程中 §11 抓到一次可复现性回归）。中心入口带来的**新**性质见 §4.1 与
> 技术路线 §7"主干必须是一棵树"。

### 4.2 手感（Q1）

| # | 问题 | 打算怎么验证 |
|---|---|---|
| Q1.1 | `max_speed = 132` 对不对？ | 技术路线 §10 的 M1 验收："拿手枪连打 10 分钟不烦躁" |
| Q1.2 | 翻滚该不该可取消？ | 现在刻意不可取消（承诺感），但没对比过 |
| Q1.3 | 无敌帧 `+0.10s` 补偿够不够？ | 用有延迟的手柄实测，看是否还会"我明明滚了却挨打" |
| Q1.4 | 加速 1300 / 摩擦 1500 的比值合适吗？ | 扫描比值 0.6~1.2，找"有惯性但不飘"的点 |

### 4.3 性能（Q4 / Q5）

| # | 问题 | 打算怎么验证 |
|---|---|---|
| Q4.1 | 每房间一个 `PointLight2D` + `LightOccluder2D`，8 个房间同时存在会掉帧吗？ | ✅ **已答（§3.11）**：5 房 / 15 灯 / 41 遮挡物，邻近开关 169.8 vs 全开 166.4 FPS。不是瓶颈 |
| Q4.2 | 子弹对象池的收益拐点在哪？ | 压测 100 / 300 / 500 发同屏，对比 `instantiate` vs 池化 |
| Q4.3 | `Room._build_tiles()` 每次重建整层的开销？ | `F9` 重建计时，随房间数增长曲线 |

### 4.4 工程

| # | 问题 | 打算怎么验证 |
|---|---|---|
| Q7.1 | 存档迁移怎么写才不烂？ | `SAVE_VERSION` 现在是 1，还没迁移过。**第一次改 schema 前先补迁移测试** |
| Q7.2 | 敌人 AI 换行为树的拐点？ | 手写 `_steer()` 超过 3 个状态、或需要复用组合时 |
| Q7.3 | `.tres` 武器表 vs 代码表，哪个更好维护？ | 现在两边都有（生成器表 + 资源文件），观察哪边先失控 |

---

## 5. 研究方法

### 5.1 改代码前先读三处

1. [docs/roadmap.md §11](docs/roadmap.md)（踩过的坑）—— 大概率已经踩过
2. 本文件 §3（已验证结论）
3. [AGENTS.md §2](AGENTS.md)（铁律）

### 5.2 验证一个引擎行为：写探针场景

Godot 的行为不要靠推理，**写个最小场景跑一次**。探针放 `tools/` 下，
命名加 `_probe` 前缀，**用完删掉**，不要提交。

模板（`tools/_probe.tscn` + `tools/_probe.gd`）：

```gdscript
extends Node

func _ready() -> void:
	_run.call_deferred()

func _run() -> void:
	await get_tree().process_frame
	# ... 打日志
	get_tree().quit()
```

运行（`--quit-after` 是保险，防止探针卡住时命令永不返回）：

```bash
$GODOT --headless --path . --quit-after 300 res://tools/_probe.tscn
```

> **注意**：探针必须是 `.tscn` 场景，不能是 `--script`——后者不加载 autoload，
> 而项目里绝大多数东西都要 autoload 才能跑。

### 5.3 验证一个游戏行为：加进 `test_run`

涉及物理、时序、空间的问题，**只有真实跑一局才暴露**（见 §3.3）。
断言写进 `tools/test_run.gd`，用 `_check(condition, label)`。

### 5.4 从 §3.4 得到的教训

**"对象存在"不等于"路径可用"。**
测试要驱动**真实入口**（`start_new_run()`、过门、死亡回菜单），
而不是只检查 autoload 非空。

新增任何"跨系统流程"时，问一句：**这条路径有测试真的走过吗？**

### 5.5 结果怎么记录

- 出了结论 → 写进 §3，附**复现命令或 `文件:行号`**
- 推翻了一个假设 → 写进 §3 并标 ❌，**不要删**，免得下次再踩
- 只是猜想 → 留在 §4 的"待验证"里，别混进 §3

---

## 6. 参考与出处

### 已引用

- 元气骑士房间生成数据：Soul Knight Wiki 社区整理
  <https://soul-knight.fandom.com/wiki/User_blog:ReChording/Room_Patterns_and_Generation>
  —— 📖 **社区 wiki，未经本项目实测**，改动生成器前请开游戏核对
- Godot 4 官方文档 <https://docs.godotengine.org/>
  —— `CharacterBody2D` / `TileMapLayer` / `PackedScene` 的权威来源

### 项目内

- [docs/roadmap.md](docs/roadmap.md) — 架构、参数、决策理由、踩坑全记录
- [docs/open-source.md](docs/open-source.md) — 素材/工具/插件与许可证
- [AGENTS.md](AGENTS.md) — 命令、铁律、已知故障
- [README.md](README.md) — 玩法、操作、已完成内容

### 待补充

以下方向值得查，但**还没有可靠出处**，不要凭印象写进来：

- 双摇杆射击的"手感"参数化方法（移速/加速度/摩擦的比值经验）
- 程序生成地牢的房间模板库设计（多少张、怎么保证连通性）
- 2D 灯光在大量 `PointLight2D` 下的性能特征

---

## 7. 变更日志

| 日期 | 变更 |
|---|---|
| 2026-09-18 | 建立本文件。记录 §3.4（测试盲区 + P0 故障）、§2（元气骑士对标）、§4（待验证清单） |
| 2026-09-18 | **里程碑 1**：修 P0（§3.4）与 P2（§3.6）；新增 §3.5 退出码、§3.7 全局状态收拢、§3.8 `ui_*` 内置动作；交付调参台（F2）；测试 96 → 121 项 |
| 2026-09-18 | **里程碑 2**：房间模板库（9 张、固定 15/21/25、不变量自检）；发现并修复瓦片层 off-by-one（§3.9）；测试 121 → 138 项 |
| 2026-09-18 | **接手核对**：`git init`（分支 `main`，基线提交含 133 文件）；`docs/` 两个文件名改 ASCII；修文档过时/错误 8 处（测试数、autoload 清单漏 `TuningPanel`、§11.4 编号断裂、§14 把已完成项列为待做、目录树缺模板脚本）；新增 §3.10（`armor_regen_rate` 死参数，**未修**）；护甲回复测试的时序余量加固 |
| 2026-09-18 | **修复 P4（§3.10）**：`armor_regen_rate` 真正生效，护盾改为延迟后按 `rate * delta` 逐点回复、与帧率无关；玩家起始 `rate = 2.0`；五个 `Health` 数值参数接入 F2 调参台；新增 2 条断言（比较式回归 + 调参台接线），测试 138 → 140 项。**回归测试做过反向验证**：改回旧实现会变红 |
| 2026-09-18 | **里程碑 3：2D 灯光（§3.11）**：`CanvasModulate` 压暗 + 玩家随身灯 + 每房 2~4 火把 + 合并矩形墙体遮挡物 + 灯光邻近开关。Q4.1 性能已实测（169.8 vs 166.4 FPS，不是瓶颈）；实测出 4.7 的 `TileMapLayer` 无 per-tile 遮挡物、新 `class_name` 需编辑器扫描两条引擎事实。`test_run` 新增 §19 五项断言，测试 140 → 145 项 |
| 2026-09-18 | **里程碑 4：Q2.2 布局重构**：5×5 网格、入口在正中、出口在边缘、主干战斗房数量成为硬约束（2 / BOSS 层 3）。修 `test_run` 小节编号重复（两个 §12，顺延后全文引用同步）。新增 §20 四项断言（走出来的最短路线而非 `_path`），测试 145 → **149** 项。过程中踩到并记下两条：主干自触会开出绕过战斗的捷径门（去掉规则后 30 种子中 6 个中招）、`Array.shuffle()` 吃全局随机破坏可复现性（§11 当场抓到） |
| 2026-09-18 | **里程碑 5：小地图（§2.2 #1 收尾）**：左上角画整层房间图 + 门连线 + 当前房/出口标记，`Level` 新增 `room_changed` 信号与 `current_room()` / `neighbors_of()` 访问器。中心入口让方向不再自明，这条把它补上——刻意不用箭头，以免把选择权收回去。`test_run` 新增 §21 五项断言（含"过一道门高亮真的跟着走"），测试 149 → **154** 项 |
| 2026-09-18 | **里程碑 6：M2 完整循环（BOSS + 每层收尾战）**：出口房从"偶数层 BOSS / 奇数层自动清空的宝箱房"改为**每层一场 BOSS 战**——旧规则下奇数层进房就白过一层。新增 `Boss`（大体型、两阶段：半血前慢速追踪 + 环形弹幕，半血后加速 + 瞄准三连发；血量 `base + per_floor × (层-1)` 成长；全部数值 `@export` 进 F2 调参台）。踩到并记下一条：**父节点 `_ready` 里改 `Health.maximum` 不影响已按旧值填充的 `current`**，必须 `set_maximum(v, true)`。`test_run` 新增 §22（真实入口驱动清房→BOSS→过层 ×3，28 项断言，cleanup 顺延为 §23），测试 154 → **182** 项。宝箱换枪保持在分支宝箱房不变 |
